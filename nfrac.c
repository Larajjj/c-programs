#include<stdio.h>
struct fraction()
{
        int numerator;
        int denominator;
}
int number()
{
        struct numerator a,struct denominator b;
        printf("enter the number of numerators to be added");
        scanf("%d",&a);
        printf("enter the number of denominators to be added");
        scanf("%d",&b);
}
void input(int n,struct f[n],struct g[n])
{
        int i;
        for (i=0;i<n;i++)
        {
                scanf("%d",&f[n]);
        }
        for (i=0;i<n;i++)
        {
                scanf("%d",&g[n]);
        }
}


#include <stdio.h>
struct fraction
{
  int numerator;
  int denominator;
};
struct fraction get_fraction_by_ret()
{
  struct fraction f;
  printf(
#include <stdio.h>
struct fraction
{
        int numerator;
        int denominator;
};
void input_fraction(struct fraction *f,struct fraction *g)
{
        printf("enter num 1:");
        scanf("%d",&f->numerator);
        printf("enter num 2:");
        scanf("%d",&f->numerator);
        printf("enter den 1:");
        scanf("%d",&g->denominator);
        printf("enter den 2:");
        scanf("%d",&g->denominator);
}
void add_fraction(struct fraction,struct fraction)
{
        int h;
        fraction->h=(f-> numerator*g-> denominator + g-> numerator*f-> denominator)
        return h;
                                                             

  
  
  #include <stdio.h>
struct fraction
{
        int numerator;
        int denominator;
};
void input_fraction(struct fraction *f,struct fraction*g)
{
        printf("enter numerator 1,denominator 1");
        scanf("%d,%d",&f->numerator,&f->denominator);
        printf("enter numerator 2,denominator 2");
        scanf("%d,%d",&g->numerator,&g->denominator);
}
struct fraction add_fraction( struct fraction f,struct fraction g)
{
        int h;
        fraction->h=(f-> numerator*g-> denominator + g-> numerator*f-> denominator)
        struct fraction h;
};
int main()
{

}
